// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'common/string_config.dart';
import 'common/text_config.dart';
import 'common/windows_registry_config.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';
import 'package:freezed_annotation/freezed_annotation.dart' hide protected;
part 'game_config.freezed.dart';

enum ConfigFileType { json, ini }

class GameAlbumConfig {
  final String id;
  final bool visible;
  final TextConfig name;
  final TextConfig? description;
  final String? icon;
  final bool requireUid;
  final String locate;
  final String toMedia;
  final String? toCover;
  final String? toThumbnail;
  final bool allowMove;
  final bool allowDelete;
  final bool allowCache;
  final Map<String, bool> chainDeletion;

  const GameAlbumConfig({
    required this.id,
    required this.visible,
    required this.name,
    this.description,
    this.icon,
    required this.requireUid,
    required this.locate,
    required this.toMedia,
    this.toCover,
    this.toThumbnail,
    required this.allowMove,
    required this.allowDelete,
    required this.allowCache,
    required this.chainDeletion,
  });

  @override
  int get hashCode =>
      id.hashCode ^
      visible.hashCode ^
      name.hashCode ^
      description.hashCode ^
      icon.hashCode ^
      requireUid.hashCode ^
      locate.hashCode ^
      toMedia.hashCode ^
      toCover.hashCode ^
      toThumbnail.hashCode ^
      allowMove.hashCode ^
      allowDelete.hashCode ^
      allowCache.hashCode ^
      chainDeletion.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is GameAlbumConfig &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          visible == other.visible &&
          name == other.name &&
          description == other.description &&
          icon == other.icon &&
          requireUid == other.requireUid &&
          locate == other.locate &&
          toMedia == other.toMedia &&
          toCover == other.toCover &&
          toThumbnail == other.toThumbnail &&
          allowMove == other.allowMove &&
          allowDelete == other.allowDelete &&
          allowCache == other.allowCache &&
          chainDeletion == other.chainDeletion;
}

class GameChannelConfig {
  final String id;
  final TextConfig name;
  final String? icon;

  const GameChannelConfig({required this.id, required this.name, this.icon});

  @override
  int get hashCode => id.hashCode ^ name.hashCode ^ icon.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is GameChannelConfig &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          name == other.name &&
          icon == other.icon;
}

class GameConfig {
  final String id;
  final TextConfig name;
  final String? icon;
  final List<GameAlbumConfig> albumsConfig;
  final List<GameChannelConfig> channelConfig;
  final WindowsGameConfig? windows;

  const GameConfig({
    required this.id,
    required this.name,
    this.icon,
    required this.albumsConfig,
    required this.channelConfig,
    this.windows,
  });

  static GameConfig? fromJson({required String json}) =>
      RustLib.instance.api.crateConfigGameConfigGameConfigFromJson(json: json);

  String toJson() =>
      RustLib.instance.api.crateConfigGameConfigGameConfigToJson(that: this);

  String toJsonPretty() => RustLib.instance.api
      .crateConfigGameConfigGameConfigToJsonPretty(that: this);

  @override
  int get hashCode =>
      id.hashCode ^
      name.hashCode ^
      icon.hashCode ^
      albumsConfig.hashCode ^
      channelConfig.hashCode ^
      windows.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is GameConfig &&
          runtimeType == other.runtimeType &&
          id == other.id &&
          name == other.name &&
          icon == other.icon &&
          albumsConfig == other.albumsConfig &&
          channelConfig == other.channelConfig &&
          windows == other.windows;
}

class WindowsGameConfig {
  final List<WindowsGameLocationConfig> locate;

  const WindowsGameConfig({required this.locate});

  @override
  int get hashCode => locate.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is WindowsGameConfig &&
          runtimeType == other.runtimeType &&
          locate == other.locate;
}

class WindowsGameConfigFileLocationConfig {
  final String key;
  final StringConfig output;
  final String? failed;

  const WindowsGameConfigFileLocationConfig({
    required this.key,
    required this.output,
    this.failed,
  });

  @override
  int get hashCode => key.hashCode ^ output.hashCode ^ failed.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is WindowsGameConfigFileLocationConfig &&
          runtimeType == other.runtimeType &&
          key == other.key &&
          output == other.output &&
          failed == other.failed;
}

class WindowsGameConfigFileSearcherConfig {
  final String path;
  final ConfigFileType configType;
  final WindowsGameConfigFileLocationConfig? toLauncher;
  final WindowsGameConfigFileLocationConfig toInstall;

  const WindowsGameConfigFileSearcherConfig({
    required this.path,
    required this.configType,
    this.toLauncher,
    required this.toInstall,
  });

  @override
  int get hashCode =>
      path.hashCode ^
      configType.hashCode ^
      toLauncher.hashCode ^
      toInstall.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is WindowsGameConfigFileSearcherConfig &&
          runtimeType == other.runtimeType &&
          path == other.path &&
          configType == other.configType &&
          toLauncher == other.toLauncher &&
          toInstall == other.toInstall;
}

class WindowsGameLocationConfig {
  final String channelId;
  final bool requireLauncher;
  final List<WindowsGameSearcherConfig> searcher;

  const WindowsGameLocationConfig({
    required this.channelId,
    required this.requireLauncher,
    required this.searcher,
  });

  @override
  int get hashCode =>
      channelId.hashCode ^ requireLauncher.hashCode ^ searcher.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is WindowsGameLocationConfig &&
          runtimeType == other.runtimeType &&
          channelId == other.channelId &&
          requireLauncher == other.requireLauncher &&
          searcher == other.searcher;
}

class WindowsGameRegistryLocationConfig {
  final WindowsRegistryConfig registry;
  final StringConfig output;
  final String? failed;

  const WindowsGameRegistryLocationConfig({
    required this.registry,
    required this.output,
    this.failed,
  });

  @override
  int get hashCode => registry.hashCode ^ output.hashCode ^ failed.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is WindowsGameRegistryLocationConfig &&
          runtimeType == other.runtimeType &&
          registry == other.registry &&
          output == other.output &&
          failed == other.failed;
}

class WindowsGameRegistrySearcherConfig {
  final WindowsGameRegistryLocationConfig? toLauncher;
  final WindowsGameRegistryLocationConfig toInstall;
  final bool useConfigFile;

  const WindowsGameRegistrySearcherConfig({
    this.toLauncher,
    required this.toInstall,
    required this.useConfigFile,
  });

  @override
  int get hashCode =>
      toLauncher.hashCode ^ toInstall.hashCode ^ useConfigFile.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is WindowsGameRegistrySearcherConfig &&
          runtimeType == other.runtimeType &&
          toLauncher == other.toLauncher &&
          toInstall == other.toInstall &&
          useConfigFile == other.useConfigFile;
}

@freezed
sealed class WindowsGameSearcherConfig with _$WindowsGameSearcherConfig {
  const WindowsGameSearcherConfig._();

  const factory WindowsGameSearcherConfig.registry(
    WindowsGameRegistrySearcherConfig field0,
  ) = WindowsGameSearcherConfig_Registry;
  const factory WindowsGameSearcherConfig.configFile(
    WindowsGameConfigFileSearcherConfig field0,
  ) = WindowsGameSearcherConfig_ConfigFile;
}
