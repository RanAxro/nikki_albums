name: Build macOS

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths-ignore:
      - '**.md'
      - 'website/**'

jobs:
  build-macos:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2

      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Disable Swift Package Manager
        run: flutter config --no-enable-swift-package-manager

      - name: Install dependencies
        run: flutter pub get

      - name: Build macOS app (arm64 only)
        run: FLUTTER_XCODE_ARCHS=arm64 flutter build macos --release

      - name: Ad-Hoc Sign macOS app
        run: codesign --force --deep --sign - build/macos/Build/Products/Release/nikki_albums.app

      - name: Zip app bundle
        run: |
          cd build/macos/Build/Products/Release
          zip -r -y nikki_albums-macos.zip nikki_albums.app

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: nikki_albums-macos
          path: build/macos/Build/Products/Release/nikki_albums-macos.zip
